# 更新记录

本文件格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [v0.4.2] - 2026-08-20

### Changed

- 内容统一收进一根居中的列。以前各页各写各的宽度、一律靠左，窗口一拉大
  右边就空一大片。现在宽度归外壳管，拉多大内容都在中间
- 首页按钮重排：四个常用操作一排，卸载单独放到下面一行并压低存在感 ——
  它跟「重新部署」并排还被挤到第二行右边，又乱又容易误点

## [v0.4.1] - 2026-08-20

### Changed

- 界面上不再显示程序目录和配置目录的完整路径，改成各给一个「打开」按钮。
  路径里夹着 Windows 用户名（`C:\Users\某某\...`），截图发到网上、贴报错日志的
  时候就跟着出去了；要看目录直接开文件管理器，比抄一串路径还方便
- README 截图按界面语言分开：四份 README 各配一套自己语言的图，
  放在 `docs/images/{en,cmn_CN,cmn_TW,jyut}/`

## [v0.4.0] - 2026-08-20

三块新东西：状态图标独立成页并能跟着任务栏深浅自动换、输入方案可以在界面上直接
切、设置能导出成 zip 换机器再导回来。另外补上了系统托盘和开机自启 —— 有了自启就
得有托盘，不然点一下 X 就没了。

### Added

- **状态图标**独立成一页，内置四套现成的（黑字 / 白字 / 蓝底白字 / 深底白字），
  每套「中 / A / 全 / 半」四个状态、五个尺寸。多一个「自动」：任务栏深色用白字、
  浅色用黑字，开机时自动对一次
- **输入方案**页顶上可以直接切当前用的方案，不用再记 Ctrl+` 那个快捷键
- **备份恢复**独立成一页：把 `*.custom.yaml`、状态图标、存的主题预设打包成 zip，
  换机器再导回来。导入前先读包里的清单弹确认，覆盖前把原文件抄一份留在配置目录
- **设置**独立成一页：开机自启、Windows 高级键盘设置入口、版本与配置目录
- **系统托盘**：关窗口只收进托盘不退出；点托盘弹的菜单是自己画的，能换方案、
  重新部署、退出

### Changed

- 输入设置改成攒够改动一次保存，不再点一下写一次盘、部署一次
- 输入设置默认跟着当前正在用的方案走，不是方案选单里的第一个
- 主窗口收到 1020×690，并且不让往小里拖 —— 再窄两栏就会掉成一栏
- Emoji 开关的选项从表情符号换成文字

### Fixed

- **选中项前那条竖杠画不出来**：颜色写成了十进制，小狼毫解析时退回
  `config_get_int`，带 alpha 的值超过 `INT_MAX` 直接失败，标记色静默变成全透明
- **深色模式下主题页调什么颜色都不生效**：小狼毫在深色模式读的是
  `style/color_scheme_dark`，我们只写了 `style/color_scheme`
- 深色模式下确认框主按钮和开关旋钮写死成白色，白底白字看不见
- 贴着滚动容器边缘的元素，焦点圈被裁掉一条边（`ring-*` 画在盒子外面）
- 开关的旋钮跑到轨道外面，把页面撑出横向溢出
- 托盘菜单飘得离鼠标老远：权限里没给 `set-size`，窗口高度压根没缩过
- 进主题预设时「用着」徽标闪一下就没了

### Removed

- 候选框预览。它是前端仿的，跟实际效果对不上，留着比没有更误导

## [v0.3.0] - 2026-08-20

主题编辑这一页重做了，另外修掉两个让整页设置形同虚设的问题 —— 深色模式下调的
颜色一个都没进过输入法，选中项前那条竖杠从来没画出来过。

### Added

- 预设主题的缩略图换成小狼毫真实画出来的候选框截图，不再是前端仿的示意图。
  配套的 `scripts/shoot-presets.py` 可以重跑：逐套写进配置、部署、敲字、抓窗口
- 预设数据抽成 `src/data/presets.json`，界面和抓图脚本共用一份
- 「其他预设」按横排 / 竖排分组

### Changed

- 主题编辑改成「预设主题」「自定义主题」两个 Tab，各自占满整页宽度；
  调色控件不再挤在 280px 的窄边栏里，按窗口宽度铺成两到三栏
- 出厂默认换成重新调过的 XGRime 浅色 / 深色

### Fixed

- **深色模式下主题页调什么颜色都不生效**。小狼毫在深色模式下读的是
  `style/color_scheme_dark`，我们只写了 `style/color_scheme`，整套配色被
  词库自带的深色方案盖掉
- **选中项前那条竖杠画不出来**。颜色写成了十进制，小狼毫解析时退回
  `config_get_int`，带 alpha 的值超过 INT_MAX 直接失败，`hilited_mark_color`
  静默变成全透明。改成 `0xAABBGGRR` 十六进制写法
- 主题页左右两栏的负外边距把内容往左挪了 4px，比标题行凸出来一截
- 顶部 Tab 条被拉满宽度但按钮挤在左边，改成整行平分

### Removed

- 候选框预览。它是前端仿的，跟实际效果对不上，留着比没有更误导
- 「XGRime 纯黑」预设 —— 新的深色底色本来就是纯黑，两套摆一起分不出

## [v0.2.0] - 2026-08-19

第一个能给人下载来用的版本。v0.1.0 只是把架子搭起来，实际跑一遍会发现装完的方案
用不了、设置存了会互相冲掉、预览跟真实候选框对不上。这一版把这些都补齐了。

### Added

- **输入方案扩到 28 个**，覆盖官方全部 `rime-*` 仓库：普通话全拼与五种双拼、注音、
  粤语拼音、五笔、仓颉、速成、快速仓颉、五笔画、行列 30、上海吴语、苏州吴语、
  中古全拼、耶鲁粤拼、香港教院式、X-SAMPA 音标、宫保拼音等
- **按「你想怎么打字」分层**：新手推荐 / 按读音打 / 按字形打 / 进阶，组内再按小类分段。
  默认停在推荐那一组
- **界面四语**：简体中文、繁體中文、廣東話、English，第一次打开跟随系统语言。
  报错也是翻译过的 —— 后端只抛错误码，人话由界面翻
- **深浅色切换**：跟随系统 / 浅色 / 深色
- **按方案配置**：装了两个以上方案时可以分别调各自的开关（简繁字形、中英标点、
  Emoji 等，读自方案自己声明的 `switches`）和模糊音，互不影响
- **更新检查**：首页比对官方稳定版；方案页可查已装方案的词库有没有更新
- **主题**：18 套预设，每套的形状都不同（圆角、留白、间距、阴影、横竖排）。
  其中 6 套是小狼毫官方内置配色，色值取自 `weasel.yaml`；另有微软输入法风格
  深浅两套、紧凑、竖排经典，以及 Dracula / Tokyo Night / Nord / Catppuccin
- **自定义预设**：调好的配色和形状可以命名存下来，随时一键换回去
- **状态图标可以自己换**：中文 / 英文 / 全角 / 半角四个状态各选一张图
- **卸载与清理**：可以卸载小狼毫 / 鼠须管，也可以备份清理卸载后残留的旧配置
- 切换中英文时那个弹出提示可以关掉或调短

### Fixed

- **装完的方案用不了**：RIME 的方案很少自给自足，`jyut6ping3.schema.yaml` 要靠另外
  四本词典做反查，少一本部署直接报错。现在逐个核对每份 `schema.yaml` 的真实依赖并
  一并安装。原先漏得最狠的三处：明月拼音和全部双拼缺笔画反查、五笔要的是
  `pinyin_simp` 而不是 `luna_pinyin`、行列缺 `opencc/emoji.json`
- **装完不会自动启用**，用户在输入法选单里根本看不到刚装的方案
- **解压过滤漏掉 `.json`**，`opencc/` 下的 emoji 与港台字形转换装不进去
- **保存输入设置会把已装方案抹掉**：整份覆盖 `default.custom.yaml`，词库页写进去的
  `schema_list` 一并没了。改为合并写入
- **候选词个数的滑杆对雾凇这类方案无效**：`menu/page_size` 的优先级是「方案 > default」，
  只改 default 等于白改
- **首页把卸载残留误判成已安装**：判定标准改成真的找得到部署器
- **检测不到 0.17 之后的小狼毫**：安装器写的是 `InstallDir` 而不是老的 `WeaselRoot`，
  且 32 位安装器会被重定向到 `WOW6432Node`
- **卸载自带方案假装成功**：注音这些的文件在输入法自己的安装目录里，删不掉也不该删。
  现在区分「装着」和「卸得掉」，自带的只给停用
- **主题预览跟真实候选框对不上**：预览只读自己写的 patch，而真正生效的是
  「程序自带 → 发行版铺的 → 我们的 patch」一整条链。现在整条都读
- **预览的外边距、字体、配色回落规则都是错的**：外边距实际是
  `max(|margin|, hilite_padding)`；`font_face` 是带 Unicode 码位的写法，
  整串塞进 CSS 会被浏览器忽略；`border_color` 回落到文字色而不是背景色。
  三处都照 `WeaselUI` 和 `RimeWithWeasel` 的源码改正
- **「高亮序号」颜色在 Windows 上一直无效**：小狼毫读的是 `hilited_label_color`，
  我们只写了鼠须管用的 `hilited_candidate_label_color`
- **预设只换颜色不换形状**：预设里写好的 `layout` 压根没被套用
- **候选框比系统输入法宽一大截**：小狼毫默认 `min_width: 160` 撑着，而这一项界面上
  根本没有。现在暴露了最小 / 最大宽度
- 保存主题会覆盖 `weasel.custom.yaml` 整份内容，用户自己加的 `app_options` 等设置被抹掉
- 读不懂的 `*.custom.yaml` 会被当成空文件覆盖，手写坏一行就丢掉整份配置
- 装方案时切走再切回来，进度条消失、列表不刷新
- 部署提示一直转圈，部署完了不吭声（部署器其实是编译完才退出的，能等到确切结果）
- 装完 / 停用 / 存设置后，成功提示要等整个部署跑完才弹出来
- 语言下拉贴在窗口底部，往下弹会被裁掉
- 配置目录写死 `%APPDATA%\Rime`，不跟随 `RimeUserDir`
- 卸载方案只删两个文件，其余残留；空目录清理会误删用户自己建的目录
- 装了多版本小狼毫时按字符串比大小，`weasel-0.9.30` 会被当成比 `0.17.4` 新
- 解压未校验条目路径，压缩包内 `../` 可写到配置目录之外
- 下载整包缓存在内存，改为流式落盘
- 翻页键设置存了读不回来

### Changed

- 整体字号上调一档，主题页在窄窗口改为上下堆叠
- 「词库管理」改名「输入方案」—— 管的本来就是方案，词库只是方案的一部分
- 每个方案显示真实下载体积，数字是实测的

## [v0.1.0] - 2026-08-19

### Added

- 首页仪表盘：自动检测 RIME 安装状态，未安装可一键下载官方安装包并启动安装
- 词库管理：内置常用方案列表，一键下载解压到配置目录，可启用 / 停用 / 卸载
- 输入设置：候选词个数、模糊音、Shift 键行为、翻页键
- 主题编辑器：候选框实时预览、配色 / 布局 / 字体调整、预设主题、横竖排切换
- 配置写入 `weasel.custom.yaml` / `squirrel.custom.yaml`，保存后自动触发重新部署
